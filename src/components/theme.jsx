import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: "#FFF"
        },
        secondary: {
            main: "#F0F"
        },
        background: {
            black: '#000',
        }
    }
})

export default theme